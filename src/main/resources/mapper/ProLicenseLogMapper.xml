<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 
 * 테이블명 : PRO_LICENSE_LOG
 * 테이블 설명 : 레슨프로자격사항 로그
-->
<mapper namespace="com.yido.clubd.repository.ProLicenseLogMapper">

    <insert id="insertProLicenseLog">
        <selectKey resultType="String" keyProperty="logSeq" order="BEFORE">
			SELECT IFNULL(CAST(MAX(LOG_SEQ) AS UNSIGNED), 0)+1 
			  FROM PRO_LICENSE_LOG
        </selectKey>    
    	INSERT INTO PRO_LICENSE_LOG (
			MS_NUM		
			, LOG_SEQ
			, LOG_DIV		
			, LIC_SEQ			
			, LIC_KIND			
			, LIC_REMARK
    	) values (
    		#{msNum}		
    		, #{logSeq}
    		, #{logDiv}	
    		, #{licSeq}		
    		, #{licKind}			
    		, #{licRemark}	
    	)  	
    </insert>
    
</mapper>